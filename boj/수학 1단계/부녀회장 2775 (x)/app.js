const fs = require('fs');
const input = fs.readFileSync(`${process.cwd()}/수학 1단계/부녀회장 2775/input.txt`).toString().trim().split('\n');
// const input = fs.readFileSync(`/dev/stdin`).toString().trim().split('\n');

// 103, 203호

